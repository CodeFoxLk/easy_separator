name: easy_separator
description: A new Flutter package for create columns and rows with separators.
version: 1.0.0
homepage: https://github.com/CodeFoxLk/easy_separator

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter: